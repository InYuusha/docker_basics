version: "3"
services:
    express-app:
        build: .
        ports:
            - "3000:3000"
    mongo:
        image: mongo
        environment:
            - MONGO_INITDB_ROOT_USERNAME=inyuusha
            - MONGO_INITDB_ROOT_PASSWORD=mypass       
        volumes: 
            - mongo-db:/data/db    
volumes:
    mongo-db:            