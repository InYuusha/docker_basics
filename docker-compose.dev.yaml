version: "3"
services:
    express-app:
        build:
            context: .
            args:
                NODE_ENV: development
        volumes:
            - ./:/app
            - /app/node_modules
        environment:
            - NODE_ENV=development    
            - MONGO_USER=inyuusha
            - MONGO_PASSWORD=mypass
        command: npm run dev    